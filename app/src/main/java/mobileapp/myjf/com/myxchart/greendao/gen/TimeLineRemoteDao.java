package mobileapp.myjf.com.myxchart.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import mobileapp.myjf.com.myxchart.entity.originaldata.TimeLineRemote;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "TIME_LINE_REMOTE".
*/
public class TimeLineRemoteDao extends AbstractDao<TimeLineRemote, Long> {

    public static final String TABLENAME = "TIME_LINE_REMOTE";

    /**
     * Properties of entity TimeLineRemote.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property OpenTime = new Property(1, long.class, "openTime", false, "OPEN_TIME");
        public final static Property Close = new Property(2, double.class, "close", false, "CLOSE");
    };


    public TimeLineRemoteDao(DaoConfig config) {
        super(config);
    }
    
    public TimeLineRemoteDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"TIME_LINE_REMOTE\" (" + //
                "\"_id\" INTEGER PRIMARY KEY ," + // 0: id
                "\"OPEN_TIME\" INTEGER NOT NULL ," + // 1: openTime
                "\"CLOSE\" REAL NOT NULL );"); // 2: close
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"TIME_LINE_REMOTE\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, TimeLineRemote entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindLong(2, entity.getOpenTime());
        stmt.bindDouble(3, entity.getClose());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, TimeLineRemote entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindLong(2, entity.getOpenTime());
        stmt.bindDouble(3, entity.getClose());
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public TimeLineRemote readEntity(Cursor cursor, int offset) {
        TimeLineRemote entity = new TimeLineRemote( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.getLong(offset + 1), // openTime
            cursor.getDouble(offset + 2) // close
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, TimeLineRemote entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setOpenTime(cursor.getLong(offset + 1));
        entity.setClose(cursor.getDouble(offset + 2));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(TimeLineRemote entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(TimeLineRemote entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
